import React from "react";

const VideoCard = ({ info }) => {
  // console.log(info);
  const { snippet, statistics } = info;
  return (
    <div>
      <img alt="" />
    </div>
  );
};

export default VideoCard;
